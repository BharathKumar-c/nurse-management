import React from 'react';

const SearchBar = ({ searchTerm, onSearchChange }) => {
  return (
    <div>
      {/* Component will be implemented in stage 3 */}
    </div>
  );
};

export default SearchBar;

