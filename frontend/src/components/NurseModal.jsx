import React from 'react';

const NurseModal = ({ isOpen, onClose, nurse, onSave }) => {
  if (!isOpen) return null;

  return (
    <div>
      {/* Component will be implemented in stage 3 */}
    </div>
  );
};

export default NurseModal;

